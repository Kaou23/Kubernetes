# Spring Boot Application Properties
spring.application.name=demo-k8s
server.port=8080

# Default message (can be overridden by environment variable APP_MESSAGE)
APP_MESSAGE=Default Hello from Application Properties
